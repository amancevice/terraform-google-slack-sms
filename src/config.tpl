{
  "aws": {
    "access_key_id": "${access_key_id}",
    "secret_access_key": "${secret_access_key}",
    "region": "${region}",
    "topic_arn": "${topic_arn}"
  },
  "slack": {
    "callback_id": "${callback_id}"
  }
}
